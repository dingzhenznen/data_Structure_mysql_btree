树这种数据结构，主要是为了解决以下几类问题：

  ## 1. 表示层级关系

  很多现实数据天然具有上下级关系，线性结构如数组、链表表示起来不直观。

  例如：

  公司
  ├── 技术部
  │   ├── 前端组
  │   └── 后端组
  └── 财务部

  适合使用树表示的场景包括：

  - 文件目录；
  - 公司组织架构；
  - HTML DOM；
  - 菜单和分类；
  - 评论回复关系；
  - XML、JSON 嵌套结构。

  ## 2. 提高搜索效率

  如果数据无序地放在数组或链表中，查找通常需要逐个比较：

  10 -> 30 -> 20 -> 50 -> 40

  查找某个值可能需要遍历全部数据，时间复杂度是：

  O(n)

  如果使用平衡二叉搜索树：

            30
          /    \
        20      50
       /       /
     10       40

  每次比较后都可以排除一部分数据，查找复杂度通常是：

  O(log n)

  ## 3. 保持数据有序

  Hash 表擅长精确查找：

  key = 100

  但不擅长范围查询：

  100 < key < 200

  搜索树中的数据按照 Key 排序，因此适合：

  - 查找大于某个值的数据；
  - 查找小于某个值的数据；
  - 范围查询；
  - 找最小值和最大值；
  - 按顺序遍历；
  - 实现排序。

  B+ 树就是数据库中用于有序搜索和范围扫描的典型树结构。

  ## 4. 支持动态插入和删除

  数组要求元素通常连续存储，中间插入或删除时需要移动大量数据。

  树通过调整节点之间的连接关系，可以动态组织数据。平衡树还可以在插入、删除后重新调整结构，保持较好的查找效率。

  ## 5. 降低磁盘访问次数

  数据库中的数据通常存储在磁盘页中。B+ 树让一个节点保存大量 Key 和指针，使树变得“矮而宽”：

  根节点
    ↓
  中间节点
    ↓
  叶子节点

  树的高度越低，查询时访问的页越少，磁盘 I/O 就越少。

  ## 6. 组织递归结构

  树本身具有递归性质：

  一棵树 = 一个节点 + 若干棵子树

  因此很多递归问题都适合用树表示：

  - 表达式计算；
  - 语法解析；
  - 文件遍历；
  - 搜索决策；
  - 路径规划；
  - AI 决策树。

  ## 与其他结构的关系

  ┌──────────┬──────────────────────────────┐
  │ 数据结构 │ 主要解决的问题               │
  ├──────────┼──────────────────────────────┤
  │ 数组     │ 连续存储、按位置快速访问     │
  │ 链表     │ 动态插入和删除               │
  │ Hash     │ 根据 Key 快速精确查找        │
  │ 树       │ 层级关系、有序查找、范围查询 │
  │ 图       │ 任意节点之间的复杂关系       │
  └──────────┴──────────────────────────────┘

  ## 一句话总结

  > 树是为了同时解决“表示层级关系”和“高效组织、搜索有序数据”的问题而出现的；不同类型的树分别优化了搜索、排序、
  > 磁盘 I/O、字符串匹配或任务调度等场景。