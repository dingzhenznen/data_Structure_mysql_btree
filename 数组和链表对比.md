## 数组和链表的对比

  ### 1. 结构差异

  #### 数组

  数组中的元素通常连续存储：

  [10][20][30][40]

  每个元素可以通过下标直接访问：

  numbers[2] // 30

  #### 链表

  链表中的节点可以分散存储，每个节点通过指针连接：

  [10 | next] -> [20 | next] -> [30 | next]

  节点结构通常是：

  typedef struct Node {
      int value;
      struct Node *next;
  } Node;

  ———

  ## 2. 操作复杂度对比

  ┌────────────┬───────────────────┬─────────────────┐
  │ 操作       │              数组 │            链表 │
  ├────────────┼───────────────────┼─────────────────┤
  │ 按下标访问 │              O(1) │            O(n) │
  │ 查找元素   │              O(n) │            O(n) │
  │ 头部插入   │              O(n) │            O(1) │
  │ 头部删除   │              O(n) │            O(1) │
  │ 尾部插入   │ 动态数组通常 O(1) │ 有尾指针时 O(1) │
  │ 中间插入   │              O(n) │ 找到位置后 O(1) │
  │ 中间删除   │              O(n) │ 找到位置后 O(1) │
  │ 内存连续性 │          通常连续 │      通常不连续 │
  └────────────┴───────────────────┴─────────────────┘

  注意：

  > 链表的插入和删除只有在已经找到目标节点或目标位置时才是 O(1)。如果还需要从头遍历寻找位置，整体仍然可能是
  > O(n)。

  ———

  ## 3. 数组的优点

  ### 访问速度快

  arr[100]

  可以直接定位第 101 个元素，不需要遍历前面的元素。

  ### 缓存友好

  数组元素通常连续存储，CPU 更容易利用缓存，因此实际运行速度通常较快。

  ### 内存开销较小

  数组只需要存储数据本身，不需要为每个元素额外保存指针。

  ———

  ## 4. 数组的缺点

  ### 中间插入和删除成本高

  例如：

  [10, 20, 30, 40]

  在 20 和 30 之间插入 25：

  [10, 20, 25, 30, 40]

  后面的元素需要移动。

  删除中间元素时，后面的元素也需要前移。

  ### 扩容可能有成本

  如果数组空间不足，动态数组可能需要：

  1. 申请更大的内存；
  2. 复制原来的元素；
  3. 释放旧空间。

  ———

  ## 5. 链表的优点

  ### 插入和删除灵活

  修改指针即可：

  原来：

  A -> C

  插入 B：

  A -> B -> C

  不需要移动大量元素。

  ### 不要求连续内存

  节点可以分散在内存的不同位置，通过指针连接起来。

  ———

  ## 6. 链表的缺点

  ### 不能快速按下标访问

  查找第 100 个节点，需要从头开始遍历：

  第 1 个 -> 第 2 个 -> 第 3 个 -> ... -> 第 100 个

  时间复杂度是 O(n)。

  ### 额外占用指针空间

  单向链表每个节点至少需要一个 next 指针，双向链表还需要一个 prev 指针。

  ### 缓存不友好

  节点可能分散在内存中，CPU 访问时容易产生缓存未命中，实际性能有时不如数组。

  ### 容易出现指针问题

  C 语言中需要手动管理内存，容易产生：

  - 空指针；
  - 野指针；
  - 内存泄漏；
  - 重复释放；
  - 链表断裂。

  ———

  ## 7. 什么时候使用数组？

  适合以下场景：

  - 经常通过下标访问；
  - 数据数量相对稳定；
  - 需要频繁遍历；
  - 需要较好的缓存性能；
  - 主要在尾部添加或删除数据。

  例如：

  int scores[100];
  scores[20] = 95;

  ———

  ## 8. 什么时候使用链表？

  适合以下场景：

  - 经常在头部或中间插入、删除；
  - 不需要频繁按下标访问；
  - 数据数量变化较大；
  - 已经持有目标节点指针；
  - 需要实现队列、LRU 缓存等结构。

  ———

  ## 9. 一个简单判断

  如果主要操作是：

  查询第几个元素

  优先考虑数组。

  如果主要操作是：

  在已有节点前后插入或删除

  可以考虑链表。

  不过在实际工程中，数组通常比链表更常用，因为数组访问速度快、内存开销小，并且很多语言运行时对数组做了优化。

  ## 总结

  > 数组擅长快速访问，链表擅长灵活插入和删除。数组的优势是连续内存和下标访问，链表的优势是节点连接和低成本修
  > 改，但链表需要额外指针并且不适合随机访问