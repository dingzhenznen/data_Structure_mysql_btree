可以都理解成“通过某个标识查找”，但这个“索引”的含义不同。

  ## 1. 数组：按照位置下标查找

  数组的索引通常表示元素在内存中的位置序号：

  int numbers[] = {10, 20, 30, 40};

  printf("%d\n", numbers[2]); // 30

  数组下标 2 表示第 3 个元素。

  数组的查找过程可以理解为：

  起始地址 + 下标 × 每个元素大小

  例如：

  numbers[2]
  = 起始地址 + 2 × sizeof(int)

  所以数组可以直接跳到目标位置，时间复杂度通常是：

  O(1)

  但是数组下标一般要求是连续的位置：

  0, 1, 2, 3, 4, ...

  ———

  ## 2. Hash：按照 Key 查找

  Hash 结构不是根据元素的物理位置查找，而是根据 Key 计算出一个位置。

  例如：

  const users = new Map();

  users.set('alice', 20);
  users.set('bob', 30);

  console.log(users.get('bob')); // 30

  查找 bob 时，Hash 结构大致会执行：

  hash("bob") -> 数字位置 -> 找到对应数据

  示意：

  "bob"
    ↓
  哈希函数
    ↓
  索引位置 5
    ↓
  找到 value=30

  因此 Hash 查找平均时间复杂度也通常是：

  O(1)

  但哈希函数可能让不同 Key 得到相同位置，这叫哈希冲突：

  hash("alice") -> 5
  hash("bob")   -> 5

  Hash 表需要通过链表、开放寻址等方式处理冲突。

  ———

  ## 3. 核心区别

  ┌────────────────┬──────────────────────┬──────────────────────┐
  │ 对比项         │ 数组                 │ Hash                 │
  ├────────────────┼──────────────────────┼──────────────────────┤
  │ 查找依据       │ 位置下标             │ Key                  │
  │ 索引是否连续   │ 通常连续             │ 不要求连续           │
  │ 典型查找       │ array[3]             │ map.get("userId")    │
  │ 平均查找复杂度 │ O(1)                 │ O(1)                 │
  │ 是否保持顺序   │ 保持元素顺序         │ 取决于具体实现       │
  │ 内存结构       │ 通常连续             │ 通常分散或通过桶组织 │
  │ 适合场景       │ 按位置访问、遍历     │ 根据 Key 快速查值    │
  │ 主要问题       │ 中间插入删除成本较高 │ 哈希冲突、扩容成本   │
  └────────────────┴──────────────────────┴──────────────────────┘

  ———

  ## 4. 一个直观比喻

  ### 数组像电影院座位

  座位 0 -> 张三
  座位 1 -> 李四
  座位 2 -> 王五

  你知道座位号 2，可以直接找到王五。

  ### Hash 像通讯录

  "张三" -> 138xxxx
  "李四" -> 139xxxx

  你根据姓名查找电话号码，姓名经过哈希计算后定位到存储位置。

  ———

  ## 5. 数组不适合当 Hash 使用的情况

  假设用户 ID 很大：

  const userId = 987654321;

  如果使用数组：

  users[987654321] = user;

  这可能浪费大量空间，因为中间的大量下标没有数据。

  使用 Hash 更合适：

  const users = new Map();

  users.set(987654321, user);

  Hash 不要求 Key 从 0 连续增长。

  ———

  ## 6. Hash 也不一定适合所有查询

  Hash 适合精确查找：

  id = 100

  但不擅长范围查询：

  id > 100
  id BETWEEN 100 AND 200

  因为 Hash 计算后，Key 的存储顺序通常是随机的。

  数组或 B+ 树更适合有序和范围查询：

  10, 20, 30, 40, 50

  可以快速找到起点，然后顺序读取。

  ## 总结

  > 数组是根据“连续位置下标”直接查找；Hash 是根据“Key 的哈希结果”定位到存储桶。两者平均查找速度都可以是 O(1)，
  > 但数组强调位置和顺序，Hash 强调通过任意 Key 快速查找数据。