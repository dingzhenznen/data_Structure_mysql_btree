“多路平衡搜索树”可以拆成三个词理解：

  ## 1. 搜索树

  树中的 Key 按大小有序排列，可以根据比较结果决定向哪个子树查找。

  例如二叉搜索树：

         50
        /  \
      30    80

  - 小于 50，去左边；
  - 大于 50，去右边。

  ## 2. 多路

  普通二叉树每个节点最多只有两个子节点，而“多路”表示一个节点可以有多个子节点。

  例如：

            [30 | 60]
          /    |     \
       <30   30~60   >=60

  这个节点有 3 个子树，因此是多路搜索树。

  一个节点可以有几十、几百甚至上千个子节点。这样树会比较“宽”，高度就会比较低。

  ## 3. 平衡

  “平衡”表示从根节点到所有叶子节点的路径长度基本一致，B+ 树中通常要求所有叶子节点处于同一层。

  不平衡的树：

          10
            \
             20
               \
                30
                  \
                   40

  它退化成了链表，查询效率可能变成 O(n)。

  平衡树：

            [20]
           /    \
        [10]    [30 | 40]

  不同数据的查询路径长度接近，因此查找效率稳定。

  ## 合起来理解

  多路平衡搜索树就是：

  > 每个节点可以拥有多个有序子节点，并且整棵树保持高度平衡的搜索树。

  B+ 树就是一种典型的多路平衡搜索树：

                [30 | 60]
               /    |    \
         [10 | 20] [30 | 40] [60 | 80]

  它的特点是：

  - 一个节点可以有多个子节点，所以叫“多路”；
  - Key 按顺序组织，所以叫“搜索树”；
  - 所有叶子节点在同一层，所以叫“平衡”；
  - 树高度较低，适合减少数据库磁盘 I/O。

  可以类比成查字典：不是每次只二选一，而是先通过一个目录页把范围分成很多段，再进入对应范围继续查找。