# MySQL InnoDB B+ 树原理

## 1. B+ 树解决什么问题

数据库数据通常存储在磁盘上，而磁盘随机读取比内存慢很多。索引的目标是减少磁盘页读取次数，并快速定位数据。

如果使用普通二叉搜索树，数据量大时树可能很高，需要多次磁盘 I/O。B+ 树通过以下方式降低树高：

1. 每个节点可以存储很多 Key；
2. 非叶子节点主要保存 Key 和子节点指针；
3. 所有真实数据都保存在叶子节点；
4. 叶子节点通常按顺序连接。

因此，B+ 树是一棵“矮而宽”的多路平衡搜索树。

## 2. B+ 树的结构

```text
                         [30 | 60]
                        /    |    \
              [10 | 20] [30 | 40] [60 | 80]
                 ↓          ↓          ↓
              数据页       数据页       数据页
```

### 根节点

根节点记录索引范围和子节点位置。例如 `[30 | 60]` 表示：

- 小于 30，进入左子树；
- 30 到 60，进入中间子树；
- 大于等于 60，进入右子树。

### 非叶子节点

非叶子节点保存索引 Key 和子节点指针，主要负责导航，不保存完整行数据。

### 叶子节点

叶子节点保存索引记录或完整数据，并按照索引值有序排列：

```text
[10] -> [20] -> [30] -> [40] -> [60] -> [80]
```

叶子节点之间的有序连接使范围查询、排序和顺序扫描非常高效。

## 3. B 树与 B+ 树的区别

| 特性 | B 树 | B+ 树 |
| --- | --- | --- |
| 数据存储位置 | 内部节点和叶子节点都可以存储 | 主要存储在叶子节点 |
| 叶子节点是否有序连接 | 不一定 | 通常通过链表连接 |
| 范围查询 | 一般 | 高效 |
| 非叶子节点能存储的 Key 数量 | 相对较少 | 更多 |
| 树高度 | 可能更高 | 通常更低 |

数据库更偏向 B+ 树，因为它适合等值查询、范围查询、排序和分页。

## 4. InnoDB 中的页

InnoDB 通常以 Page 作为磁盘读写的基本单位，默认页大小常见为 `16 KB`。一个索引页可以保存大量 Key 和指针，因此即使表中有数百万或数千万行，B+ 树高度通常也不会很高。

一次典型索引查询大致经过：

```text
根节点
  ↓
非叶子节点
  ↓
叶子节点
  ↓
找到索引记录
```

根节点和部分上层节点可能已经缓存在 InnoDB Buffer Pool 中，所以实际查询的磁盘 I/O 次数可能比树高更少。

## 5. 一次索引查询的过程

假设存在索引：

```sql
CREATE INDEX idx_user_name ON user(name);
```

执行：

```sql
SELECT *
FROM user
WHERE name = 'Alice';
```

查询过程如下：

1. 从根节点开始比较 `name`；
2. 根据 Key 的范围进入对应子节点；
3. 逐层向下，直到叶子节点；
4. 在叶子节点中找到 `Alice`；
5. 如果是二级索引，可能还需要根据主键回表获取完整数据。

## 6. InnoDB 的聚簇索引

InnoDB 的主键索引是一棵特殊的 B+ 树，称为聚簇索引。

```sql
CREATE TABLE user (
    id BIGINT PRIMARY KEY,
    name VARCHAR(100),
    age INT
);
```

主键 B+ 树的叶子节点保存完整数据行：

```text
[id=1, name='Alice', age=20]
[id=2, name='Bob',   age=30]
[id=3, name='Carol', age=25]
```

因此，执行下面的查询时，找到主键记录后即可得到完整数据：

```sql
SELECT *
FROM user
WHERE id = 2;
```

聚簇索引的主要特点：

- 数据行按照主键顺序组织；
- 主键索引叶子节点保存完整行；
- 主键查询通常只需要一次 B+ 树查找；
- 主键越大，二级索引中保存的主键值也越大。

## 7. InnoDB 的二级索引与回表

对于普通二级索引，叶子节点通常保存“索引列 + 主键值”，而不是完整行：

```text
name='Alice' -> id=1
name='Bob'   -> id=2
name='Carol' -> id=3
```

执行：

```sql
SELECT *
FROM user
WHERE name = 'Bob';
```

大致过程是：

```text
二级索引 B+ 树
    ↓ 找到 name='Bob'
得到主键 id=2
    ↓
主键聚簇索引
    ↓
得到完整数据行
```

根据二级索引得到主键后，再到聚簇索引中查找完整行，这个过程称为“回表”。

## 8. 覆盖索引

如果查询需要的字段已经全部包含在二级索引中，就不需要回表。

```sql
CREATE INDEX idx_name_age ON user(name, age);

SELECT name, age
FROM user
WHERE name = 'Alice';
```

索引中已经包含 `name` 和 `age`，查询可以直接从索引返回结果，这叫覆盖索引，也常称为 Index Only Scan。

覆盖索引可以减少回表，但索引字段不能无限增加，因为索引越宽：

- 占用空间越大；
- 一个页能存放的记录越少；
- 树可能变高；
- 插入和更新成本越高。

## 9. 为什么 B+ 树适合范围查询

假设叶子节点如下：

```text
[10] -> [20] -> [30] -> [40] -> [50] -> [60]
```

对于：

```sql
SELECT *
FROM user
WHERE id BETWEEN 20 AND 50;
```

数据库先通过树找到 `20`，然后沿叶子节点链表顺序读取，直到 `50` 为止，不需要为每个值重新从根节点查找。

这类查询通常适合 B+ 树：

```sql
WHERE id BETWEEN 100 AND 200
WHERE create_time >= '2026-01-01'
ORDER BY id
LIMIT 20
```

## 10. 联合索引与最左匹配原则

假设建立联合索引：

```sql
CREATE INDEX idx_abc ON t(a, b, c);
```

它的排序方式类似字典排序：先按 `a`，`a` 相同时按 `b`，`a` 和 `b` 都相同时按 `c`。

因此，下面的条件可以较好地使用该索引：

```sql
WHERE a = ?
WHERE a = ? AND b = ?
WHERE a = ? AND b = ? AND c = ?
```

而只查询 `b`：

```sql
WHERE b = ?
```

通常无法充分利用 `(a, b, c)` 的有序结构，因为跳过了最左列 `a`。

范围条件也会影响后续列的定位能力，例如：

```sql
WHERE a = 1
  AND b > 10
  AND c = 3;
```

通常可以使用 `a` 做等值匹配、使用 `b` 做范围扫描，但 `c` 的索引定位能力可能下降。设计联合索引时，常见经验是把等值条件放前面，把范围条件放后面，最终应结合 `EXPLAIN` 和实际数据分布判断。

## 11. B+ 树中的插入与删除

### 插入

插入记录时，数据库会：

1. 从根节点找到目标叶子页；
2. 按顺序插入记录；
3. 如果页面已满，执行页分裂；
4. 将新的边界 Key 写入父节点；
5. 如果父节点也已满，继续向上分裂。

递增主键通常会让新记录追加到最右侧叶子页，页分裂相对较少。随机 UUID 可能让记录插入任意位置，更容易产生随机 I/O、页分裂和索引碎片。

### 删除

删除记录后，页面可能出现空间空洞。当页面利用率过低时，数据库可能进行记录重新分布、页面合并或调整父节点边界。删除一条数据并不意味着整棵树会立即重建。

## 12. 为什么有索引却不一定使用

优化器会根据成本选择执行计划，有索引不代表一定走索引。常见原因包括：

### 匹配行太多

如果条件的选择性很低，走索引后仍要读取大量数据，全表扫描可能更快。

### 对索引列使用函数

不推荐：

```sql
WHERE YEAR(create_time) = 2026
```

可以改写为范围条件：

```sql
WHERE create_time >= '2026-01-01'
  AND create_time <  '2027-01-01'
```

### 前置模糊匹配

下面的条件通常难以利用普通 B+ 树索引：

```sql
WHERE name LIKE '%Alice'
```

而下面的前缀匹配通常可以利用索引：

```sql
WHERE name LIKE 'Alice%'
```

### 返回数据量过大

如果查询几乎需要整张表的数据，优化器可能选择全表扫描而不是索引扫描。

建议使用 `EXPLAIN` 检查实际执行计划，而不是仅凭 SQL 形式判断是否使用了索引。

## 13. B+ 树与 Hash 索引

| 查询类型 | B+ 树 | Hash |
| --- | --- | --- |
| 等值查询 `=` | 支持 | 很适合 |
| 范围查询 `>`、`<`、`BETWEEN` | 支持 | 不适合 |
| `ORDER BY` | 支持 | 不支持 |
| 前缀匹配 | 部分支持 | 不支持 |
| 范围扫描 | 高效 | 不支持 |

InnoDB 的普通索引主要使用 B+ 树。InnoDB 还可能根据访问模式建立自适应哈希索引，但这属于内部优化机制，不等同于用户创建的普通 Hash 索引。

## 14. 总结

可以把 InnoDB 的索引理解为：

```text
主键索引：
B+ 树叶子节点 = 完整数据行

二级索引：
B+ 树叶子节点 = 索引字段 + 主键值
                         ↓
                    必要时回表
```

核心优势：

1. 树高度低，磁盘 I/O 少；
2. Key 有序，适合范围查询；
3. 叶子节点相连，适合排序和顺序扫描；
4. 多路结构适合磁盘页；
5. 聚簇索引和二级索引配合完成高效查询。

一句话概括：

> MySQL InnoDB 使用 B+ 树把索引 Key 按顺序组织在多个页中，通过从根节点逐层定位到叶子节点，实现低磁盘 I/O 的等值查询、范围查询和有序扫描；主键索引叶子节点保存完整数据，二级索引通常保存索引列和主键，必要时还要回表。
