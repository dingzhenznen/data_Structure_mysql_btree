 联合索引和覆盖索引有什么区别


• 联合索引和覆盖索引不是同一个概念，它们关注的角度不同。

  ## 1. 联合索引：索引的定义方式

  联合索引是指一个索引包含多个列。

  CREATE INDEX idx_user_name_age
  ON user(name, age);

  这个索引包含：

  name + age

  它主要解决的是：

  > 如何根据多个字段建立有序的索引结构。

  索引的排序顺序是：

  先按 name 排序
  name 相同时再按 age 排序

  因此它适合：

  WHERE name = 'Alice'
  WHERE name = 'Alice' AND age = 20
  ORDER BY name, age

  需要遵循最左匹配原则，通常不能只依赖：

  WHERE age = 20

  因为跳过了最左列 name。

  ———

  ## 2. 覆盖索引：查询是否需要回表

  覆盖索引不是一种特殊的索引类型，而是描述一种查询状态：

  > 查询所需的全部字段，都已经包含在某个索引中，因此不需要回表。

  例如表结构：

  CREATE TABLE user (
      id BIGINT PRIMARY KEY,
      name VARCHAR(100),
      age INT,
      email VARCHAR(200),
      INDEX idx_name_age(name, age)
  );

  执行：

  SELECT name, age
  FROM user
  WHERE name = 'Alice';

  idx_name_age 中已经包含：

  name、age

  查询需要的字段也只有：

  name、age

  所以可以直接从二级索引返回结果，不需要再查聚簇索引。这就是覆盖索引。

  ———

  ## 3. 联合索引不一定是覆盖索引

  同一个联合索引：

  CREATE INDEX idx_name_age
  ON user(name, age);

  对于下面的查询：

  SELECT name, age
  FROM user
  WHERE name = 'Alice';

  它是覆盖索引。

  但对于：

  SELECT name, age, email
  FROM user
  WHERE name = 'Alice';

  索引中没有 email，所以需要回表：

  二级索引 -> 找到主键 -> 聚簇索引 -> 获取 email

  此时它仍然是联合索引，但不是这个查询的覆盖索引。

  ———

  ## 4. 单列索引也可以实现覆盖索引

  覆盖索引不要求一定是联合索引。

  例如：

  CREATE INDEX idx_name ON user(name);

  执行：

  SELECT name
  FROM user
  WHERE name = 'Alice';

  索引中已经有 name，因此也可以覆盖这个查询。

  所以：

  联合索引：一个索引包含多个列
  覆盖索引：某次查询所需字段都在索引中

  ———

  ## 5. 二级索引中的主键列

  InnoDB 的二级索引叶子节点通常包含：

  索引列 + 主键值

  例如：

  INDEX(name)

  叶子节点可以理解为：

  name='Alice' -> id=1
  name='Bob'   -> id=2

  因此，即使没有显式把 id 写入联合索引：

  CREATE INDEX idx_name_age ON user(name, age);

  下面的查询通常也可以被覆盖：

  SELECT id, name, age
  FROM user
  WHERE name = 'Alice';

  因为二级索引中有：

  name、age，以及隐含的主键 id

  但如果查询需要其他字段，例如 email，仍然需要回表。

  ———

  ## 6. 对比总结

  ┌────────────────────┬──────────────────┬───────────────────────────────┐
  │ 对比项             │ 联合索引         │ 覆盖索引                      │
  ├────────────────────┼──────────────────┼───────────────────────────────┤
  │ 关注点             │ 索引包含多个列   │ 查询是否可以只查索引          │
  │ 是否是索引类型     │ 是一种索引定义   │ 是一种查询效果                │
  │ 是否一定避免回表   │ 不一定           │ 通常可以                      │
  │ 是否必须包含多个列 │ 是               │ 不必须                        │
  │ 示例               │ INDEX(name, age) │ SELECT name, age WHERE name=? │
  └────────────────────┴──────────────────┴───────────────────────────────┘

  ## 一句话理解

  > 联合索引讲的是“索引里面有哪些列”；覆盖索引讲的是“这次查询需要的列是否全部在索引里”。

  两者可以同时出现：

  INDEX(name, age)

  既是联合索引，也可能成为某些查询的覆盖索引。