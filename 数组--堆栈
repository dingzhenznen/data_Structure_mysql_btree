如果使用数组会在堆中分配空间吗


• 不一定。数组是否在堆中分配，取决于数组的声明方式。

  ### 1. 局部数组：通常在栈中

  int main(void)
  {
      int numbers[10];
  }

  numbers 是局部数组，通常分配在 main 函数的栈空间中，函数结束后自动释放。

  栈
  ┌──────────────┐
  │ numbers[0]   │
  │ numbers[1]   │
  │ ...          │
  │ numbers[9]   │
  └──────────────┘

  不需要调用 free。

  ———

  ### 2. 全局数组：不在堆中

  int numbers[10];

  全局数组通常位于静态存储区，程序启动时创建，程序结束时释放。

  static int numbers[10];

  static 数组也通常位于静态存储区。

  ———

  ### 3. 使用 malloc 创建数组：在堆中

  #include <stdlib.h>

  int *numbers = malloc(10 * sizeof(int));

  if (numbers == NULL) {
      return 1;
  }

  numbers[0] = 100;

  free(numbers);
  numbers = NULL;

  这里：

  栈                         堆
  numbers  ───────────────>  10 个 int 的空间

  - numbers：指针变量，通常在栈上
  - malloc 申请的数组空间：在堆上
  - 使用完必须调用 free

  ———

  ### 4. 使用 calloc 创建数组：也在堆中

  int *numbers = calloc(10, sizeof(int));

  calloc 也在堆中申请数组，并且会把内存初始化为 0。

  free(numbers);

  ———

  ### 总结

  ┌───────────────────────┬──────────────────┬───────────────┐
  │ 写法                  │ 数组通常所在位置 │ 是否需要 free │
  ├───────────────────────┼──────────────────┼───────────────┤
  │ int a[10]; 局部数组   │ 栈               │ 否            │
  │ int a[10]; 全局数组   │ 静态存储区       │ 否            │
  │ static int a[10];     │ 静态存储区       │ 否            │
  │ int *a = malloc(...); │ 堆               │ 是            │
  │ int *a = calloc(...); │ 堆               │ 是            │
  └───────────────────────┴──────────────────┴───────────────┘

  需要注意，严格来说 C 标准并没有规定“栈”和“堆”这两个具体实现区域。实际编译器通常按照上面的方式分配