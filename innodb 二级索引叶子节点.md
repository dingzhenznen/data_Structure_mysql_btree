 InnoDB 的二级索引 的非叶子节点村什么

 ## 简短答案

  InnoDB 二级索引的非叶子节点主要存储：

  二级索引 Key + 子节点页号

  也就是：

  索引键值 + 指向下一级索引页的指针

  它不存储完整数据行，也通常不是用来直接定位数据行的。

  ## 示例

  假设表结构：

  CREATE TABLE user (
      id BIGINT PRIMARY KEY,
      name VARCHAR(100),
      age INT,
      INDEX idx_name(name)
  );

  二级索引 B+ 树可以简化表示为：

                      [Bob | Tom]
                     /     |     \
                    /      |      \
               页号 P10  页号 P11  页号 P12

  非叶子节点中的记录可以理解为：

  name='Bob' -> 子节点页 P10
  name='Tom' -> 子节点页 P11

  这里的 P10、P11、P12 是指向下一级索引页的页指针。

  ## 叶子节点和非叶子节点的区别

  ### 非叶子节点

  二级索引 Key + 子节点页号

  作用：

  导航、缩小查找范围

  例如：

  name='Bob' -> 去某个子页继续查找

  ### 叶子节点

  二级索引叶子节点保存：

  二级索引列 + 主键值

  例如：

  name='Alice' -> id=1
  name='Bob'   -> id=2
  name='Tom'   -> id=3

  InnoDB 官方文档说明，二级索引记录包含二级索引列和对应行的主键列；InnoDB
  还会把主键列追加到二级索引中，作为索引顺序和定位的一部分。(dev.mysql.com
  (https://dev.mysql.com/doc/refman/8.4/en/innodb-index-types.html?utm_source=openai))

  ## 完整查询过程

  执行：

  SELECT *
  FROM user
  WHERE name = 'Bob';

  查询路径是：

  二级索引非叶子节点
      ↓
  根据 name 找到目标叶子页
      ↓
  二级索引叶子节点找到：
  name='Bob' -> id=2
      ↓
  根据 id=2 查询聚簇索引
      ↓
  得到完整数据行

  因此：

  非叶子节点：帮助找到叶子页
  叶子节点：找到主键值
  聚簇索引：根据主键找到完整数据

  ## 为什么非叶子节点不存完整数据？

  因为非叶子节点只负责“导航”。如果它们也保存完整行：

  - 每个节点能存放的 Key 会减少；
  - B+ 树可能变高；
  - 查询需要访问更多页；
  - 索引占用空间更大。

  所以 B+ 树把完整数据尽量放在叶子节点，内部节点只保存足够导航的信息。

  ## 与主键索引对比

  ┌──────────────┬─────────────────────┬─────────────────────────┐
  │ 节点类型     │ 主键聚簇索引        │ 二级索引                │
  ├──────────────┼─────────────────────┼─────────────────────────┤
  │ 非叶子节点   │ 主键 Key + 子页指针 │ 二级索引 Key + 子页指针 │
  │ 叶子节点     │ 完整数据行          │ 二级索引列 + 主键值     │
  │ 是否需要回表 │ 不需要              │ 查询其他列时通常需要    │
  └──────────────┴─────────────────────┴─────────────────────────┘

  一句话总结：

  > 二级索引的非叶子节点主要保存“二级索引键值和子节点页指针”，用于逐层导航；真正的主键值通常在叶子索引记
  > 录中，用于后续回到聚簇索引查找完整数据。