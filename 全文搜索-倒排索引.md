## 一、什么是全文搜索

全文搜索用于在大量文本中查找包含某个词或短语的文档，例如：

- 搜索文章内容
- 搜索商品名称和描述
- 搜索日志、评论和网页正文

它关注的是“文本内容中是否出现相关词语”，而不是像普通数据库索引那样只对完整字段值进行匹配。

## 二、什么是倒排索引

倒排索引是一种根据“关键词”反向查找“文档”的索引结构。

普通存储关系是：

```text
文档 -> 文档中的词
```

倒排索引则反过来：

```text
词 -> 包含这个词的文档列表
```

例如有三篇文档：

```text
文档1：MySQL 使用 B+Tree
文档2：MySQL 使用倒排索引
文档3：Redis 使用跳表
```

建立倒排索引后：

```text
MySQL     -> 文档1、文档2
使用      -> 文档1、文档2
B+Tree    -> 文档1
倒排索引  -> 文档2
Redis     -> 文档3
```

## 三、倒排索引的组成

倒排索引通常由两部分组成。

### 1. 词典

词典保存所有出现过的词，例如：

```text
MySQL、B+Tree、倒排索引、Redis
```

词典本身通常还会建立索引，以便快速定位关键词。

### 2. 倒排列表

倒排列表记录每个词出现在哪些文档中，有时还会记录：

- 文档编号
- 词出现的位置
- 词出现的次数
- 词的权重
- 文档的其他元信息

例如：

```text
MySQL -> [
  {docId: 1, positions: [1]},
  {docId: 2, positions: [1]}
]
```

## 四、全文搜索的查询过程

用户搜索“`MySQL 倒排索引`”时，系统通常会执行以下步骤：

1. 对查询内容进行分词。
2. 在词典中找到 `MySQL` 和 `倒排索引`。
3. 分别获取两个词对应的倒排列表。
4. 对文档列表进行交集、并集等集合运算。
5. 根据词频、词语位置和权重计算文档相关性。
6. 按相关性排序并返回结果。

## 五、为什么倒排索引查询速度快

没有倒排索引时，搜索通常需要逐篇读取文档、分词，并判断是否包含关键词：

```text
逐条读取每篇文档 -> 分词 -> 判断是否包含关键词
```

数据量越大，这种全表扫描的成本越高。

有了倒排索引后，系统可以直接根据关键词找到相关文档，避免扫描全部文本，因此特别适合大量文档的搜索场景。

## 六、倒排索引和 B+Tree 的区别

| 对比项 | B+Tree 索引 | 倒排索引 |
| --- | --- | --- |
| 索引方向 | 字段值 -> 数据行 | 关键词 -> 文档 |
| 适合场景 | 等值、范围、排序查询 | 文本内容搜索 |
| 典型查询 | `id = 10`、`age > 20` | 搜索包含“数据库”的文章 |
| 是否需要分词 | 通常不需要 | 通常需要 |
| 相关性排序 | 一般不负责 | 通常支持 |
| 典型应用 | MySQL 普通索引 | Elasticsearch、Lucene、全文索引 |

## 七、倒排索引的代价

倒排索引也会带来额外成本：

- 需要占用额外的磁盘和内存。
- 文档新增、修改和删除时需要维护索引。
- 分词质量会影响搜索效果。
- 索引构建和更新会消耗 CPU。
- 复杂查询需要合并多个倒排列表。

## 八、总结

全文搜索通过分词理解文本内容，倒排索引则把“关键词对应哪些文档”保存下来，使系统能够从关键词快速定位相关文档，而不必逐篇扫描全文。

可以把它概括为：

```text
原始文档 -> 分词 -> 建立“词 -> 文档列表”的映射 -> 快速查询和相关性排序
```
