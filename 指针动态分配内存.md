
`  struct Books
  {
    int   book_id;
  };`

  int main(void)
  {
      struct Books *Book1 = malloc(sizeof(struct Books));

      if (Book1 == NULL) {
          return 1;
      }

      Book1->book_id = 6495700;

      printBook(Book1);

      free(Book1);
      return 0;
  }

  动态内存版本还需要：

  #include <stdlib.h>